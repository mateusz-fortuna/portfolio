import { Content } from "../../../public/content";
import styles from "../../../styles/Projects.module.sass";

const DesktopView = ({ image, section }: Content) => (
  <div className={styles.projects__desktopView}></div>
);

export default DesktopView;
