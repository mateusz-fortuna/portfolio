import styles from "../../../styles/Menu.module.sass";

const Icon = () => (
  <>
    <span className={styles.menu__icon_span} />
    <span className={styles.menu__icon_span} />
    <span className={styles.menu__icon_span} />
  </>
);

export default Icon;
